{
  "name": "weather-demo-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js backend that resolves supported cities and fetches current weather via Azure Maps.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
